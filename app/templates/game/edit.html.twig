{% include 'default/_form.html.twig' with {
    page_title: 'title.game_edit',
    page_title_param: {'%id%': game.id},
    attr: {
        method: 'PUT',
        action: url('game_edit', {'id': game.id}),
    },
    back_to_list_path: 'games_index',
} %}
{% include 'default/_form.html.twig' with {
    page_title: 'title.add_admin',
    page_title_param: {'%id%': user.id},
    attr: {
        method: 'PUT',
        action: url('add_admin', {id: user.id}),
    },
    back_to_list_path: 'admin_index',
} %}